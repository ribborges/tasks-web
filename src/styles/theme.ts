// Theme variables

const font = {
    family: "'Ubuntu', sans-serif",
    size: {
        default: "100%"
    },
    weight: {
        default: "normal"
    }
};

const color = {
    accent_primary: "#0000ff",
    accent_secondary: "#7a00eb",
    dark: "#0e0e0e",
    dark_grey: "#222222",
    grey: "#444444",
    light_grey: "#dddddd",
    light: "#eeeeee"
};

const radius = {
    small: "5px",
    medium: "10px",
    large: "20px"
};

const fx = {
    blur: "blur(15px)",
    transition: "1s"
}

export { font, color, radius, fx};